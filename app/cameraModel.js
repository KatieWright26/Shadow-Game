import playerModel from './playerModel'

export class cameraModel {
	constructor() {
		this.positionX = 0,
		this.positionY = 0,
		this.cameraLocation = this.playerModel
		console.log(this.cameraLocation)
	}
}